name: Windows Build Environment Setup

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  setup-build-env:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run setup_build_env.ps1
        shell: pwsh
        run: |
          ./setup_build_env.ps1
